import React from 'react';
import styled from 'styled-components';

const HeaderWrapper = styled.div`
  text-align: center;
`;

const Header = () => {
  return <HeaderWrapper>{/* <h2>Site Title</h2> */}</HeaderWrapper>;
};

export default Header;
